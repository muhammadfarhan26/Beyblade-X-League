import React from 'react';
import { Box, Typography } from '@mui/material';

const BeybladeCustomizer = ({ onSave, playerBeyblade }) => {
  // This is a stub component to prevent build errors
  // The full implementation would be added later
  return (
    <Box sx={{ p: 2 }}>
      <Typography variant="body1">Beyblade Customizer Component</Typography>
      <Typography variant="body2" color="text.secondary">
        This feature is coming soon!
      </Typography>
    </Box>
  );
};

export default BeybladeCustomizer; 